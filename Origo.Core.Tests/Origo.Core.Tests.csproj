<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <OutputType>Exe</OutputType>
        <RollForward>LatestMajor</RollForward>
        <IsPackable>false</IsPackable>
        <IsTestProject>true</IsTestProject>
        <UseAppHost>true</UseAppHost>
        <!-- Test methods use underscore naming convention by design. -->
        <!-- CA1707: test method underscores; CA1861: inline arrays in assertions; CA1859: intentional interface-typed variables in architecture/contract tests -->
        <NoWarn>$(NoWarn);CA1707;CA1861;CA1859</NoWarn>
    </PropertyGroup>

    <PropertyGroup>
        <!-- Coverlet: enforce Origo.Core line coverage in CI and local test runs for this project. -->
        <CollectCoverage>true</CollectCoverage>
        <CoverletOutputFormat>cobertura</CoverletOutputFormat>
        <Include>[Origo.Core]*</Include>
        <!-- Reflection-heavy bootstrap: excluded from line % gate; covered indirectly via LoadAndSpawnFromFile / guard tests. -->
        <ExcludeByFile>**/Runtime/OrigoAutoInitializer.cs,**/Addons/FastNoiseLite/FastNoiseLite.cs,**/*.g.cs</ExcludeByFile>
        <!-- CompilerGeneratedAttribute is intentionally NOT excluded: it would
             silently remove async/iterator state-machine bodies from the
             coverage denominator if Origo.Core ever gains such methods. -->
        <ExcludeByAttribute>Obsolete,GeneratedCodeAttribute</ExcludeByAttribute>
        <Threshold>90</Threshold>
        <ThresholdType>line</ThresholdType>
        <ThresholdStat>total</ThresholdStat>
    </PropertyGroup>

    <ItemGroup>
        <PackageReference Include="coverlet.msbuild">
            <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
            <PrivateAssets>all</PrivateAssets>
        </PackageReference>
        <PackageReference Include="Microsoft.NET.Test.Sdk"/>
        <PackageReference Include="xunit.v3"/>
        <PackageReference Include="xunit.runner.visualstudio">
            <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
            <PrivateAssets>all</PrivateAssets>
        </PackageReference>
    </ItemGroup>

    <ItemGroup>
        <ProjectReference Include="..\Origo.Core\Origo.Core.csproj"/>
        <ProjectReference Include="..\Origo.TestSupport\Origo.TestSupport.csproj"/>
    </ItemGroup>

</Project>
