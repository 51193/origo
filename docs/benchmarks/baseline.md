# TypedData 性能基线

> 本文件是 `TypedData` 源生成产物与无优化装箱实现的性能基线，作为后续性能优化的权威对照来源。
> 数值与运行环境、运行时版本强相关，跨机器不可直接比较；优化前后须在**同一环境同一运行时**下复测。

## 复现

```bash
bash scripts/benchmark.sh
```

该脚本依次运行两套基准（均标记 `[Trait("Category","Benchmark")]`，从 `ci.sh` 排除，仅此处运行一次）：

- **SG 纯净微基准** — `Origo.SourceGeneration.Tests/Benchmarks/TypedDataGeneratedBenchmarkTests.cs`
- **Core 真实模拟基准** — `Origo.Core.Tests/Benchmarks/TypedDataRealWorldBenchmarkTests.cs`

## 采样元信息

| 项 | 值 |
|----|----|
| 日期 | 2026-06-26 |
| CPU | AMD Ryzen 7 9700X（8C/16T，最高 5.58 GHz，运行时 scaling ≈ 75%） |
| 内存 / OS | 30 GiB / Ubuntu 26.04 LTS（Linux 7.0.0-22-generic） |
| .NET | SDK 10.0.109，运行时 **10.0.9**（测试目标 `net8.0`，经 roll-forward 运行于 .NET 10） |
| 构建 | `Release` |
| 采样 | 4 轮完整运行串行执行取均值；每个微基准内部已取 `min of 5` |
| 倍率口径 | `Ratio` = 快者吞吐 / 慢者吞吐；「胜方」标注更快的一侧 |

## 方法学

- **时间**：固定容量池 + 位掩码寻址（内存恒定）、大迭代次数（单轮跨多个 OS 时间片）、1 轮 warmup + 多轮取两侧各自最小耗时（剔除被抢占/GC 的离群轮）。
- **分配**：每个基准在计时轮之外，对每一侧各跑一次专用 `[MethodImpl(NoInlining)]` 测量轮，取 `GC.GetAllocatedBytesForCurrentThread()` 前后差值。测量被刻意放在独立方法里，使计时循环保留未插桩版的代码生成，避免插桩污染吞吐。
- **Alloc 列**：记每轮（即表中迭代数对应的一轮）的实测分配，格式「生成 / 装箱」。

> **「Alloc」是本基线中最稳定、最权威的信号**：跨 4 轮逐字一致。绝对吞吐受 CPU 调频与超平凡循环的代码对齐影响存在抖动（见末尾效度局限），相对趋势与分配量才是优化的可靠依据。

## SG 纯净微基准

### 写入吞吐 — 生成 operator vs 装箱 class（2,000,000 迭代）

| 类型 | 生成 (Mops/s) | 装箱 (Mops/s) | 倍率 | 胜方 | Alloc 生成/装箱 |
|------|--------------:|--------------:|:----:|:----:|----------------|
| Int32   | 909 | 70.1 | 13.0x | 生成 | **0 B / 106.81 MB** |
| Int64   | 906 | 72.9 | 12.4x | 生成 | 0 B / 106.81 MB |
| Single  | 885 | 72.0 | 12.3x | 生成 | 0 B / 106.81 MB |
| Double  | 882 | 72.0 | 12.2x | 生成 | 0 B / 106.81 MB |
| Boolean | 895 | 71.8 | 12.5x | 生成 | 0 B / 106.81 MB |
| Char    | 902 | 72.4 | 12.5x | 生成 | 0 B / 106.81 MB |
| String（ref slot） | 576 | 111 | 5.2x | 生成 | 0 B / 61.04 MB |

> 写入的优势主要来自**避免分配**：生成路径原地构造结构体（0 B），装箱路径每次 `new` 类实例（值类型还额外装箱），2M 次写入产生 ~107 MB（String 无装箱，仅类实例 ~61 MB）。

### 读取吞吐 — 生成 TryGet/Kind vs 装箱 `is T`（10,000,000 迭代，两侧 Alloc 均 0 B）

| 类型 | 生成 (Mops/s) | 装箱 (Mops/s) | 倍率 | 胜方 | 稳定性 |
|------|--------------:|--------------:|:----:|:----:|--------|
| Int32   | 551 | 580 | 1.05x | 装箱 | 稳 |
| Int64   | 552 | 583 | 1.06x | 装箱 | 稳 |
| Single  | 560 | 574 | 1.02x | 装箱 | 近平手 |
| Double  | 559 | 582 | 1.04x | 装箱 | 稳 |
| Boolean | 529 | 582 | 1.10x | 装箱 | 稳 |
| Char    | 535 | 583 | 1.09x | 装箱 | 稳 |
| String（`TryGetString`） | 408 | 584 | 1.43x | 装箱 | 高方差（生成 395–418） |
| String（`IsString`） | 588 | 576 | 1.02x | 生成 | 稳 |

### 混合分派 — 生成 Kind switch vs 装箱 `is T`（10,000,000 迭代，两侧 0 B）

| 场景 | 生成 (Mops/s) | 装箱 (Mops/s) | 倍率 | 胜方 | 稳定性 |
|------|--------------:|--------------:|:----:|:----:|--------|
| 混合分派（int/float/bool/string/double） | ~1217 | 807 | 1.51x | 生成 | 中等方差（1.42–1.62x） |

## Core 真实模拟基准

### 异构字典迭代（2,048,000 次 `.Data` 读取）

| 场景 | 生成 .Data (Mops/s) | 装箱迭代 (Mops/s) | 倍率 | 胜方 | Alloc 生成/装箱 |
|------|--------------------:|------------------:|:----:|:----:|----------------|
| 异构字典 `.Data` 迭代 | 412 | ~2925 | ~7.1x | 装箱 | **37.49 MB / 0 B** |

> 这是全基线最大短板，且根因被分配数据坐实：`.Data → TypedDataObjectConverter.ToObject` 对值类型**重新装箱**（数据集 ~80% 为值类型），每轮产生 37.49 MB；装箱字典直接返回已有引用（0 B、近内存带宽上限）。时间倍率本轮在 6.3–8.2x 间抖动（装箱侧是最快循环、对调频/对齐极敏感），**37.49 MB 分配是更稳定可靠的退化/改进度量**。

### 工厂构造 + 字典插入（500,000 迭代）

| 类型 | 生成 (Mops/s) | 装箱 (Mops/s) | 倍率 | 胜方 | Alloc 生成/装箱 |
|------|--------------:|--------------:|:----:|:----:|----------------|
| String  | 188 | 146 | 1.29x | 生成 | 23.53 MB / 14.97 MB |
| Int32   | 227 | 73.8 | ~3.1x | 生成 | 23.53 MB / 26.42 MB |
| Single  | 225 | 71.3 | ~3.2x | 生成 | 23.53 MB / 26.42 MB |
| Boolean | 226 | 69.3 | ~3.3x | 生成 | 23.53 MB / 26.42 MB |

> 分配揭示一处权衡：值类型插入时装箱侧多出 ~12 MB 装箱（26.42 vs 23.53），生成更快且更省；但 **String 插入生成侧反而多分配**（23.53 vs 14.97 MB）——`Dictionary<string,TypedData>` 的每个 entry 内嵌 24 字节结构体，使后备数组比 `Dictionary<string,object>`（8 字节引用）更大。生成速度仍领先，但内存占用更高。

### 观察者通知（2,000,000 迭代，两侧 0 B）

| 场景 | 生成 (Mops/s) | 装箱 (Mops/s) | 倍率 | 胜方 | 稳定性 |
|------|--------------:|--------------:|:----:|:----:|--------|
| Observer notify (old,new) + `.Data is string` | ~2790 | ~5530 | 1.98x | 装箱 | 极稳（1.98x） |

> 无分配差异，差距纯来自 **24 字节结构体按值传递/搬运**（引用仅 8 字节）。

### 字典查找 TryExtract（2,000,000 迭代，两侧 0 B）

| 类型 | 生成 (Mops/s) | 装箱 (Mops/s) | 倍率 | 胜方 | 稳定性 |
|------|--------------:|--------------:|:----:|:----:|--------|
| String  | 192 | 175 | 1.09x | 生成 | 稳 |
| Int32   | 114 | 160 | 1.41x | 装箱 | 稳 |
| Single  | 114 | 156 | 1.37x | 装箱 | 稳 |
| Boolean | 112 | 149 | 1.33x | 装箱 | 稳 |

### 多类型强转链 float→int→long→double（2,000,000 迭代，int payload，两侧 0 B）

| 场景 | 生成 (Mops/s) | 装箱 (Mops/s) | 倍率 | 胜方 | 稳定性 |
|------|--------------:|--------------:|:----:|:----:|--------|
| 数值强转链 | 352 | 250 | 1.41x | 生成 | 稳（4 轮 1.40–1.41x） |

## 优化优先级（按差距、可信度与分配证据排序）

| 优先级 | 热点 | 现状 | 分配证据 | 备注 |
|--------|------|------|---------|------|
| P0 | 异构字典 `.Data` 迭代 | 装箱快 ~7.1x | 生成 **37.49 MB**/轮（重新装箱） | 分配是稳定可靠的度量；消除 `.Data` 热路径装箱收益最高 |
| P1 | 观察者通知 | 装箱快 1.98x | 两侧 0 B | 纯结构体按值传递成本；可试 `in` 传递 |
| P1 | 值类型单读 / DictLookup 值类型 | 装箱快 1.05–1.41x | 两侧 0 B | 24 字节结构体缓存足迹 vs 8 字节引用 |
| P2 | String `TryGetString` 读取 | 装箱快 1.43x | 两侧 0 B | 高方差，先稳定测量再定位 |
| 观察 | String 插入内存占用 | 生成更快但多分配 23.53 vs 14.97 MB | — | 结构体内嵌使字典后备数组更大 |

### 当前优势区（优化时勿回归）

- 值类型写入 ~12–13x 且零分配、String 写入 5.2x、字典构造+插入 ~3x、混合分派 1.51x、强转链 1.41x。

## 效度局限

1. **CPU 动态调频（scaling ≈ 75%）** 引入运行间抖动；针对 ≤ 1.3x 的边际项与最快循环（如异构迭代装箱侧 6.3–8.2x、写入），建议固定频率/性能模式后复测。
2. **绝对吞吐受代码对齐影响**：写入等超平凡循环对方法布局/对齐敏感，存在 ~±8% 系统性偏移；**分配量与相对趋势不受此影响**，应作为主要判据。
3. **运行时为 .NET 10.0.9**（非目标 `net8.0`）；优化结论须在同一运行时下复测。
4. **微基准取 min-of-rounds**，偏向理想 JIT 稳态，掩盖尾延迟；真实模拟套件更具代表性。
5. 已知高方差项：String `TryGetString` 读取、混合分派、异构迭代装箱侧、值类型插入装箱侧；优化前应增加采样轮数收敛。
