# Origo 策略协作

实体行为由挂载在实体上的策略协作形成。以下术语描述策略之间的执行顺序。

## 领域词汇

**生命周期策略（Lifecycle Strategy）**：参与实体帧处理和实体生命周期的被动策略。
_Avoid_：用“策略”泛指所有可排序行为

**策略顺序约束（Strategy Ordering Constraint）**：声明一个生命周期策略先于或后于另一个生命周期策略的关系，不表示目标策略必须挂载。
_Avoid_：策略依赖、优先级

**已挂载策略（Mounted Strategy）**：当前参与某个实体行为的策略；策略可注册而未挂载到该实体。
_Avoid_：已注册策略
**实体策略顺序（Entity Strategy Order）**：满足已注册生命周期策略间直接与传递顺序约束的实体执行顺序，不依赖策略的挂载先后。
_Avoid_：插入顺序、数字优先级顺序